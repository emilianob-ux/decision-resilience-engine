# Desarrollo y CI (pip install -r requirements.txt -r requirements-dev.txt)
-r requirements.txt
pytest>=8.0,<9
# Formateador pineado exacto: `ruff format` cambia entre versiones menores y
# `ruff format --check` en CI falla si local, pre-commit y CI usan versiones distintas.
# Mantener sincronizado con .pre-commit-config.yaml y pyproject [dev].
ruff==0.16.7
pre-commit>=4,<5
pydantic>=2,<3
scipy>=1.11,<2
fastapi>=0.115,<1
uvicorn>=0.30,<1
httpx>=0.27,<1
redis>=5,<6
fakeredis>=2,<3
